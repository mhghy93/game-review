import {
  ADMIN_LOGIN,
  ADMIN_LOGIN_SUCCESS,
  ADMIN_LOGIN_FAIL,
  ADMIN_LOADED,
  ADMIN_AUTH_ERROR,
  SHOW_ALL_USERS,
  DELETE_USER_REVIEW,
  DELETE_USER_ACCOUNT,
} from './types';

export const loadAdmin = () => async (dispatch) => {
  try {
  } catch (err) {}
};

export const adminLogin = (email, password) => async (dispatch) => {
  try {
  } catch (err) {}
};
